# Objective

Move the experimental Kafka exporter from its current state toward production-ready.

This is a **high-level tracking issue**. It enumerates the focus areas at a high level; each area is intended to be broken out into its own **sub-issue / subtask** that goes deeper, carries the detailed work, and is closed independently. Progress on the exporter is tracked by the state of those child subtasks.

# Rationale

The exporter has a broad configuration surface, docs, and solid unit coverage. Core runtime paths (topic routing, encoding, partitioning, ACK/NACK reporting, deadline-bounded shutdown flush/purge) are now exercised by in-process integration tests running against `rdkafka::mocking::MockCluster` (librdkafka's built-in mock), so they run in CI by default with no Docker dependency. The mock broker is the primary vehicle for integration testing; several areas (failure/recovery, auth, TLS, backpressure, performance) still need coverage extended on top of it.

The exporter today sends **one message per pdata, awaited synchronously** on a single task -- there is no application-level sending queue, bounded concurrency, or delivery-future pipelining (`exporter.rs:524-530`, module TODO `exporter.rs:6-7`). Retry is delegated to a separate upstream `processor:retry` node rather than handled in-process. Several correctness and security gaps are documented in-code as TODOs (`topic_router.rs:21-22`, `partitioner.rs:93-99`, `producer.rs:587`, `exporter.rs:6-7`). This document makes those explicit so they don't get lost.

# Scope

Each numbered area below is a **child subtask** to be tracked separately. Each lists what to cover at a high level, the relevant **code anchors**, any **known gaps** already visible in the implementation, and its own **acceptance criteria**. The detailed breakdown lives in the corresponding sub-issue; solutions are intentionally left open where non-obvious.

> Applicability vs. the Kafka receiver issue: the exporter is a **sink**, so the receiver's "offset guarantees" and "consumer-group rebalancing" areas have **no exporter analog** (no consumer offsets, no consumer group) and are intentionally omitted. The receiver's lifecycle (drain/shutdown) area maps to section 2 here, its telemetry area to section 7, and its "routing & payload correctness" and "failure recovery" areas to sections 5 and 6 (mixed-signal, malformed-payload, header propagation, and prolonged-outage recovery are covered there).

## 1. Security

Constrain dynamic topic routing, prevent uncontrolled topic creation, avoid sensitive-header fingerprinting, and sanitize adversarial values in telemetry.

- [x] **Dynamic topic routing is unconstrained.** A transport-header value can select an arbitrary destination topic with no allowlist, prefix constraint, or ACL check (`topic_router.rs` `TopicRouter::resolve`; priority 1 = header, priority 2 = static config). The only guard is Kafka topic-name syntax validation (`validate_kafka_topic`). (Done: added a per-signal operator allowlist -- `SignalConfig::allowed_topics` (exact names) and `allowed_topics_regex` (operator-provided regex patterns matched as-is, OR semantics) in `config.rs`, enforced in `TopicRouter::resolve` after syntax validation; a disallowed header topic returns `KafkaExporterError::DisallowedHeaderTopic` and is permanently nacked with no static fallback. Regex patterns are compiled once into `Option<Vec<Regex>>` fields on the `KafkaExporter` struct in `new()` (and rebuilt on reconfigure) -- never per message -- and threaded into the router; `None` avoids allocating for the unconstrained case. Empty lists preserve the previous unrestricted behavior. Covered by topic_router unit tests (`test_allowed_regex_permits_matching_header_topic`, `test_regex_allowlist_matches_pattern_as_provided`, `test_disallowed_header_topic_is_rejected_without_fallback`, `test_exact_allowlist_permits_listed_header_topic`, `test_exact_or_regex_allowlist_combined`, `test_no_constraint_allows_any_valid_header_topic`, `test_allowlist_does_not_constrain_static_topic`), config tests (`allowlist_config_is_accepted`, `invalid_exact_allowlist_entry_is_rejected`, `invalid_regex_allowlist_entry_is_rejected`), and exporter integration tests (`disallowed_dynamic_topic_is_permanently_nacked`, `allowed_dynamic_topic_is_delivered`).)
- [x] **Uncontrolled topic creation.** There is no first-class control over broker-side auto-creation; `allow.auto.create.topics` is only reachable through the `producer_config` escape hatch. Combined with header-driven routing, a client that controls the routing header can cause a broker configured to auto-create to spawn arbitrary topics. (Done: added a first-class `allow_auto_create_topics` field defaulting to `false` (default-deny), always written to the client config in `build_client_config`, and added `allow.auto.create.topics` to `MANAGED_PRODUCER_CONFIG_KEYS` so a passthrough override is superseded and warned. Covered by `build_client_config_defaults_auto_create_to_false`, `build_client_config_allows_auto_create_when_opted_in`, `auto_create_key_is_managed_and_first_class_field_wins`.)
- [x] **Sensitive-header fingerprinting.** `partition_key_from_transport_headers` folds each transport header's **name and value** into a single `Xxh64` hash to derive the partition key. The emitted key is a stable, deterministic 16-char hash (not plaintext), but it makes traffic from a given tenant/token fingerprintable via partition-assignment analysis. (Done/recorded decision: the deterministic non-plaintext-hash tradeoff is accepted and documented in the `partitioner.rs` module doc and the README Security section (co-locating a tenant's data is the feature; disable `partition_by_transport_headers` for null-key round-robin). The "hash, never plaintext" property is pinned by `partition_key_never_contains_raw_header_value_or_name` and `partition_key_is_stable_hash_for_same_sensitive_header`.)
- [x] **Adversarial values in telemetry.** Client- or tenant-controlled values reach logs unsanitized: resolved topic in `kafka.exporter.send.failed`; invalid header topic in `kafka.exporter.topic.invalid_header`; downstream nack reason in `effect_handler.info("Kafka exporter: received Nack - ...")`. (Done: added `sanitize_for_log` (`common/kafka/mod.rs`) which escapes control characters and bounds length to `MAX_SANITIZED_LOG_LEN`, applied at all three sites -- the send-failed topic, the invalid/disallowed-header topic in `TopicRoutingError`, and the nack-reason log. Values stay visible but bounded/escaped. Covered by `sanitize_for_log_passes_through_benign_value`, `sanitize_for_log_escapes_control_characters`, `sanitize_for_log_truncates_overlong_value`. Confirmed SASL/TLS secrets are only set into the rdkafka config and never logged.)

**Acceptance criteria:**

- [x] A routing-constraint mechanism (allowlist / prefix / regex / ACL) is implemented or a decision to defer is recorded. (Done: per-signal exact allowlist (`allowed_topics`) and regex allowlist (`allowed_topics_regex`, patterns matched as provided) implemented in `config.rs` / `topic_router.rs`, with regexes pre-compiled on the exporter struct.)
- [x] Uncontrolled-topic-creation posture is decided (gated or explicitly documented). (Done: first-class `allow_auto_create_topics`, default-deny, managed key.)
- [x] Sensitive-header fingerprinting risk in the partition-key derivation is triaged with a recorded decision. (Done: accepted tradeoff documented in `partitioner.rs` + README; non-plaintext property test-pinned.)
- [x] Adversarial values (topic, nack reason) in logs are bounded/sanitized or the exposure is explicitly accepted. (Done: `sanitize_for_log` applied at all three log sites.)

## 2. Shutdown & live reconfiguration

Verify deadline-aware cancellation, draining, flushing, producer-thread termination, and replacement while deliveries are in flight.

- [x] **Shutdown path** (`exporter.rs:548-560`): info log -> cancel the periodic-telemetry timer -> `drain_and_flush(deadline, ...)` -> return `TerminalState::new(deadline, [self.metrics.snapshot()])`. (Done: exercised by the `shutdown_flushes_buffered_records` and new `shutdown_flushes_under_sustained_traffic` integration tests in `exporter.rs`, which drive the full Shutdown arm and assert the terminal flush delivers all buffered records.)
- [x] **Deadline-bounded drain/flush** (`drain_and_flush`, `exporter.rs:473-498`): `flush_timeout = deadline - now` saturating to `Duration::ZERO` (`483-485`); `producer.flush(flush_timeout)` (`487`); **only on flush failure/timeout** -> `otel_warn!("kafka.exporter.shutdown.flush_failed", ...)` (`488-491`) then `producer.purge(queue().inflight())` (`495-496`). (Done: validated by the new `shutdown_honors_deadline_when_broker_unavailable` test in `exporter.rs`, which points the exporter at an unroutable broker, buffers a record, and asserts the deadline-bounded flush+purge returns well within a generous outer timeout instead of hanging; purge-on-error path is thereby exercised.)
- [x] **Producer-thread termination.** The custom `Drop` (`producer.rs:203-224`) sets `should_stop = true` and **joins** the 1-second poll thread (`producer.rs:212-214`); on join failure -> `otel_warn!("kafka.exporter.producer.poll_thread_join_failed", ...)` (`producer.rs:216-219`). **`Drop` does not flush or purge** -- draining is entirely the responsibility of `drain_and_flush` before the exporter returns. (Done: poll-thread teardown is now exercised on two paths -- at shutdown by the shutdown tests, and mid-run by the live-reconfigure build-and-swap in `KafkaExporter::reconfigure` (`exporter.rs`), where dropping the old producer joins its poll thread; covered by `reconfigure_switches_topic` and `reconfigure_flushes_inflight_before_swap`.)
- [x] **Drain relies on engine receiver-first ordering.** There is no explicit `DrainIngress` handling in the loop; unrecognized control messages fall through the `Control(_)` catch-all (`exporter.rs:561-563`) with a comment noting ingress is already closed by the engine's receiver-first drain (`exporter.rs:552-555`). (Done: `shutdown_honors_deadline_when_broker_unavailable` confirms a broker-unavailable shutdown cannot hang past the deadline, and `shutdown_flushes_under_sustained_traffic` confirms the drain holds under sustained traffic.)

**Live reconfiguration is now handled (build-and-swap).** The engine defines `NodeControlMsg::Config { config }` for pushing a new configuration to a running node (`control.rs:261-264`). The exporter previously ignored it via the `Control(_)` catch-all; it now handles it in a dedicated event-loop arm that calls the new `KafkaExporter::reconfigure` method (`exporter.rs`). Reconfiguration deserializes/validates the incoming JSON into a `KafkaExporterConfig`, builds a replacement librdkafka producer via the shared `KafkaExporter::build_producer` helper (extracted from `KafkaExporter::new` so the AWS-gated client-context selection lives in one place), performs a **bounded drain of the old producer** (flush bounded by the old config's `timeout_ms`, then purge of anything still queued so a slow/unavailable broker cannot stall the swap), and only then swaps in the new producer and config. Records already in flight get one bounded final chance to deliver before the swap. Reconfiguration is best-effort: an invalid config or a producer-build failure is logged (`kafka.exporter.reconfigure_error`) and the existing producer keeps running, matching the warn-and-keep posture of sibling nodes (condense-attributes and retry processors).

**Acceptance criteria:**

- [x] Deadline-aware cancellation, drain, flush, and poll-thread termination validated under sustained traffic, with a bounded deadline and no pipeline hang when the broker is unavailable -- `exporter.rs:473-498`, `producer.rs:203-224`. (Done: covered by `shutdown_flushes_under_sustained_traffic` (sustained traffic) and `shutdown_honors_deadline_when_broker_unavailable` (bounded deadline, unavailable broker) in `exporter.rs`.)
- [x] `Config`-based live reconfiguration is supported (build-and-swap with a bounded in-flight drain) or explicitly deferred with a recorded decision -- `exporter.rs:561-563`, `control.rs:261-264`. (Done: implemented in `KafkaExporter::reconfigure` + the `NodeControlMsg::Config` event-loop arm in `exporter.rs`; covered by `reconfigure_switches_topic`, `reconfigure_flushes_inflight_before_swap`, and `reconfigure_with_invalid_config_keeps_running`.)

## 3. Retry correctness

Validate integration with the Retry Processor, including transient/permanent error classification, retry exhaustion, and duplicate-delivery behavior.

- [x] **The exporter has no internal retry loop** (beyond librdkafka's queue-full retry, `producer.rs:416-446`, which sleeps 100ms on `QueueFull` at `producer.rs:423`). Transient retry is delegated to a separate `processor:retry` node placed **upstream** of the exporter (`README.md:355-363`, example wiring `README.md:414-418`). (Confirmed: no in-exporter retry; the exporter's terminal contribution is a Nack classification, validated by the tests below.)
- [x] **Error classification** (validate each path emits the correct nack kind):
  - Kafka **send failure** -> **transient** nack `reporter.nack(...)` (`exporter.rs:441-464`, `inc_failed` at `442`);
  - **encoding failure** -> **permanent** nack (`exporter.rs:405-407`);
  - **unconfigured signal type** -> **permanent** nack (`exporter.rs:354-356`);
  - **invalid dynamic topic** from a header -> **permanent** nack (`exporter.rs:369-373`, `TopicRoutingError`).
  Permanent vs. transient is realized via `NackMsg::new_permanent` vs. `NackMsg::new` in `EffectHandlerReporter` (`exporter.rs:118-136`). (Done: send-failure -> transient covered by `send_failure_reports_transient_nack`; encoding-failure -> permanent by the new `encoding_failure_is_permanently_nacked` (invalid-UTF-8 array attribute forces the OTAP CBOR conversion to fail); unconfigured-signal -> permanent by `test_export_unconfigured_signal_type_is_nacked`; invalid dynamic topic -> permanent by `test_export_invalid_dynamic_topic_is_permanently_nacked` and `disallowed_dynamic_topic_is_permanently_nacked`. These assert on the two-vector transient/permanent split of `RecordingReporter`. The real `EffectHandlerReporter` mapping is additionally validated end-to-end below.)
- [x] **Retry exhaustion.** After `max_elapsed_time`, the retry processor forwards a final nack; data is dropped at the source with no dead-letter queue (`README.md:433-434`). (Done/characterized: a stand-in retry processor drops a permanent nack in `permanent_nack_is_not_retried` -- nothing is produced to any topic and the batch counts as exactly one `logs_failed` with zero `logs_exported`, characterizing drop-at-source / no-DLQ. The exporter itself does not implement `max_elapsed_time`; that bound lives in the out-of-process `processor:retry` node.)
- [x] **Duplicate delivery / ordering.** Because the retry processor retries out-of-band, a later batch may be sent and acked before an earlier batch still being retried (`README.md:432`); a send that timed out or failed after the broker persisted it can also produce a duplicate on retry. Characterize the resulting duplication and ordering and confirm it is bounded. (Done/characterized: `retried_transient_send_duplicates_bounded` drives a stand-in retry loop that re-sends on each transient nack and asserts the number of delivered copies is bounded by the number of retry attempts (at-least-once). NOTE: the true persisted-despite-reported-failure duplicate window (a record the broker persisted before an ack was lost to a network partition) cannot be reproduced by the in-process `MockCluster` -- every rejected produce is genuinely not persisted -- so a clean retry yields exactly one copy; the documented limitation matches `produce_failure_is_bounded_and_not_persisted_on_mock` and requires a real broker.)

**Retry support is out-of-process, by design.** A `Nack` classification is the exporter's terminal contribution; the actual retry/backoff lives in the separate `processor:retry` node (`README.md:355-363`). The work here is to **confirm** that path end-to-end via mock-broker integration testing rather than to add retry logic to the exporter. (Done: since a real `processor:retry` cannot be wired in a unit test, the `KafkaExporterHarness` now exposes the node's Ack/Nack unwind (completion) channel via `recv_nack`, and tests act as a stand-in retry processor -- reading the routed `NackMsg` emitted by the real `EffectHandlerReporter`, inspecting its `permanent` flag, and re-sending the returned `refused` pdata on a transient nack (or dropping it on a permanent one). Pdata carries a NACKS+RETURN_DATA subscriber unwind frame so the nack routes to a subscriber.)

**Acceptance criteria:**

- [x] Transient vs. permanent classification validated end-to-end with an upstream `processor:retry` node -- `exporter.rs:354-356`, `369-373`, `405-407`, `441-464`. (Done: end-to-end through the real `EffectHandlerReporter` by `transient_nack_reaches_retry_on_send_failure` (a send failure unwinds a non-permanent nack carrying the refused pdata) and `permanent_nack_reaches_retry_on_disallowed_topic` (a disallowed dynamic topic unwinds a permanent nack); plus the unit-level classification tests listed above.)
- [x] Retry-exhaustion (final nack, drop-at-source) and duplicate-delivery/ordering behavior characterized and shown to be bounded -- `README.md:432-434`. (Done: `permanent_nack_is_not_retried` (drop-at-source, no DLQ, one `logs_failed`), `transient_nack_retried_until_success_then_acked` (a retried transient batch eventually delivers with no data loss), and `retried_transient_send_duplicates_bounded` (redelivery bounded by retry-attempt count; real-broker limitation documented).)

## 4. Backpressure & resource bounds

Exercise bounded concurrency, queue saturation, prolonged broker outages, and memory behavior under sustained load.

- [x] **Serial, inline send.** The event loop handles one pdata per iteration and awaits it to full delivery before the next `recv()` (`exporter.rs:524-530`); there is no `FuturesUnordered`, bounded concurrency, or delivery-future pipelining. This is flagged in-code: `exporter.rs:6-7` -- `//! ToDo: Currently only handles one kafka message add a time we should` / `//! improve the throughput by handling delivery futures`. (Done: replaced the serial inline send with bounded delivery-future pipelining. `export_pdata` was split into `enqueue_pdata` (encode + `ExporterFutureProducer::send_result`, returning a delivery future + `SendMeta`) and `finalize_send_completion` (ack/nack on resolution). The event loop now tracks deliveries in a bounded `InFlightSends` (`FuturesUnordered`) capped by the new `max_in_flight` config, with a backpressure guard, opportunistic drain, and a biased completion-vs-recv select mirroring the OTLP gRPC exporter -- all in `exporter.rs`. The module TODO is removed. Covered by `pipelined_sends_all_deliver_and_ack`, `pipelined_preserves_per_partition_order_with_keys`, and `default_max_in_flight_preserves_serial_ordering`.)
- [x] **No application-level sending queue.** There is no `sending_queue` / `num_consumers` / in-memory queue backpressure (`README.md:345-353`, gap table `README.md:430`); batching, lingering, and buffering are delegated to librdkafka via `linger_ms` (`config.rs:464`) and `producer_config` queue knobs (e.g. `queue.buffering.max.messages`, `batch.num.messages`), which are only reachable through the escape hatch. (Done: `max_in_flight` (`config.rs`, default 1, rejects 0) is the first-class bounded sending window. When the in-flight set is full the loop parks the next pdata and only drains completions, so intake stops and backpressure propagates upstream -- confirmed by `bounded_concurrency_caps_in_flight_and_delivers` (stalled broker + bounded cap, all delivered) and documented in the README "Backpressure and concurrency" section.)
- [x] **Queue saturation.** librdkafka `QueueFull` triggers an in-producer retry with a 100ms sleep (`producer.rs:416-446`); confirm this interacts sanely with the serial loop and the pipeline's own backpressure rather than unbounded stalling. (Done/characterized: the pipelined path uses `send_result`, which does not retry a full queue; an enqueue `QueueFull` (or any enqueue error) is reported as a transient nack (`enqueue_pdata`) so the upstream `processor:retry` resends, and the bounded in-flight set keeps the queue from being driven unboundedly deep. Characterized by `enqueue_failure_reports_nack_without_tracking`, which bursts against a 1-deep producer queue and asserts every batch is accounted (success+failure, none lost) and the loop keeps running. NOTE: a deterministic `QueueFull` cannot be forced on the in-process mock -- librdkafka drains its queue on the 1s poll cycle -- so the test asserts the accounted-and-healthy invariant rather than a guaranteed rejection; a deterministic queue-full requires a real broker with a controllable send rate.)
- [x] **Prolonged broker outage.** With bounded `message.timeout.ms` (see section 8), each send fails within the deadline and becomes a transient nack; validate memory stays bounded (no unbounded in-flight accumulation) across a sustained outage. (Done: `prolonged_outage_keeps_pipelined_sends_bounded` injects a produce outage under `max_in_flight = 4` and asserts every pipelined send is accounted exactly once (success + failure == sends, none leaks or double-counts) with at least one accounted failure. NOTE: because librdkafka coalesces pipelined records into a broker-chosen number of produce requests, the mock's per-request error injection cannot deterministically map errors to individual sends, so the exact success/failure split is not asserted here; the serial-path `recovers_after_prolonged_produce_outage` covers the exact one-request-per-send split.)

**Acceptance criteria:**

- [x] Behavior under queue saturation, prolonged broker outage, and sustained load is documented; memory stays bounded; backpressure propagation to the pipeline is confirmed -- `exporter.rs:524-530`, `producer.rs:416-446`. (Done: documented in the README "Backpressure and concurrency" section; memory-bounded + backpressure confirmed by `bounded_concurrency_caps_in_flight_and_delivers`, `prolonged_outage_keeps_pipelined_sends_bounded`, `shutdown_drains_pipelined_in_flight_sends`, and the queue-saturation characterization `enqueue_failure_reports_nack_without_tracking`.)
- [x] The bounded-concurrency / delivery-future direction is triaged with a recorded decision (behavior-preserving default) -- `exporter.rs:6-7`. (Done: implemented as bounded delivery-future pipelining with a behavior-preserving default of `max_in_flight = 1` (serial order preserved; `default_max_in_flight_is_one`, `default_max_in_flight_preserves_serial_ordering`). The direction is quantified by the `max_in_flight` benchmark sweep (see section 9).)

## 5. Delivery semantics

Test ACK/NACK propagation, ordering, partitioning, timeouts, and potentially-persisted messages across broker and network failures.

- [x] **Delivery is awaited via the delivery callback.** `ExporterFutureProducer::send` enqueues the record and awaits a oneshot resolved from the librdkafka delivery callback (`producer.rs:395-447`; callback -> `tx.send(...)` at `producer.rs:574-588`, with `producer.rs:587` -- `let _ = tx.send(owned_delivery_result); // TODO: handle error`). So each send resolves on final delivery/failure, not on enqueue. (Done: `send_success_reports_ack` and `send_failure_reports_transient_nack` in `exporter.rs` assert the outcome is decided by the resolved delivery -- an ack only after a successful callback and a transient nack only after a failure callback -- rather than on enqueue.)
- [x] **ACK/NACK propagation.** Success -> `inc_exported` + `reporter.ack(...)` (`exporter.rs:427-440`); failure -> `inc_failed` + transient `reporter.nack(...)` (`exporter.rs:441-464`). Downstream Ack/Nack observed by the loop update `acks_received` / `nacks_received` (`exporter.rs:537-547`). (Done: classification asserted at unit level via `export_once` + `RecordingReporter` in `send_success_reports_ack` (one ack, no nacks) and `send_failure_reports_transient_nack` (one transient nack, no permanent, no ack); the broker-backed counters are asserted via the terminal `logs.exported`/`logs.failed` in `delivery_success_increments_exported` and `produce_failure_increments_failed`.)
- [x] **Timeouts.** Send timeout is `Duration::from_millis(self.config.timeout_ms())` (`exporter.rs:425`), mapped to librdkafka `message.timeout.ms` (`config.rs:454`) and validated to `(0, 30_000]` (see section 8) so the await is always bounded. (Done: `send_times_out_within_bound_when_broker_unavailable` in `exporter.rs` points the send at an unroutable broker with a short `timeout_ms` and asserts the delivery await resolves as a failure well within a generous outer bound, i.e. it never hangs.)
- [x] **Partitioning.** Partition key is derived from transport headers when enabled (`partitioner.rs:80-91`) and applied to the record only when present (`exporter.rs:416-419`); the librdkafka partitioner strategy is configurable (`config.rs:788-825`, applied at `config.rs:467`). (Done: `same_partition_key_maps_to_stable_partition` sends many same-key records to a 4-partition topic and asserts they all land on one partition with the documented header-derived key (key-to-partition stability); `null_key_distributes_evenly_across_partitions` asserts keyless records reach every partition in a near-even split (round-robin).)
- [x] **Potentially-persisted messages.** A send that fails or times out after the broker has already persisted the record yields a transient nack and a possible duplicate on retry; validate this across broker restarts, leader elections, and network partitions. (Done/characterized: `recovers_across_broker_restart_and_leader_reassignment` exercises a broker restart with explicit leader reassignment and asserts no accepted record is lost and post-restart records are delivered; `produce_failure_is_bounded_and_not_persisted_on_mock` characterizes the produce-failure outcome as a bounded transient failure. `recovers_after_prolonged_produce_outage` extends this to a sustained outage: a bounded run of consecutive produce rejections all count as `messages{logs,failure}` and none persist, and the first post-outage send is delivered and consumed (`messages{logs,success} == 1`), so the exporter recovers after a prolonged outage without stalling. NOTE: the true persisted-despite-reported-failure duplicate window (record persisted before an ack lost to a network partition) cannot be reproduced by the in-process `MockCluster` and requires a real broker; this is documented in the test.)

**Acceptance criteria:**

- [x] ACK/NACK propagation, ordering, partitioning, and timeout behavior validated, including duplicate/at-least-once behavior for messages persisted despite a reported failure, across broker and network failures -- `exporter.rs:425-464`, `producer.rs:395-447`, `partitioner.rs:80-91`. (Done: ACK/NACK -> `send_success_reports_ack`, `send_failure_reports_transient_nack`, `delivery_success_increments_exported`, `produce_failure_increments_failed`; ordering -> `preserves_per_partition_order` (monotonic per-partition offsets in send order); partitioning -> `same_partition_key_maps_to_stable_partition`, `null_key_distributes_evenly_across_partitions`; timeouts -> `send_times_out_within_bound_when_broker_unavailable`; broker/leader failures -> `recovers_across_broker_restart_and_leader_reassignment`. The persisted-despite-failure duplicate window is characterized via `produce_failure_is_bounded_and_not_persisted_on_mock` with a documented mock limitation requiring a real broker for a true duplicate.)

## 6. Kafka integration

Add broker-backed success and failure tests covering supported encodings, acknowledgements, compression, TLS, SASL, and AWS MSK IAM.

- [x] **Encodings.** `OtlpProto` via `encode_to_otlp_bytes` (`encoder.rs:39-50`) and `OtapProto` via `encode_to_batch_arrow_record_bytes` (`encoder.rs:63-77`); the message-format header (`MessageFormat`, values `otlp`/`otap`) is always written (`exporter.rs:285-293`, constants `common/kafka/mod.rs:361-375`). (`OtlpJson` is not implemented -- `common/kafka/mod.rs:207-209`.) (Done: OTLP happy path for all three signals (`exports_logs_otlp_to_mock_broker`, `exports_traces_otlp_to_mock_broker`, `exports_metrics_otlp_to_mock_broker`, each asserting payload bytes + `assert_format_otlp`); OTAP encoding for all three signals (`exports_logs_otap_sets_otap_format_header`, `exports_traces_otap_sets_otap_format_header`, `exports_metrics_otap_sets_otap_format_header`, each asserting `assert_format_otap` + a `BatchArrowRecords::decode`); broker-side durability for both encodings (`otlp_and_otap_payloads_persist_on_broker` via `inspect().message_count`); per-signal produce-failure paths (`logs_send_failure_increments_failed`, `traces_send_failure_increments_failed`, `metrics_send_failure_increments_failed`); and `otlp_json_message_format_is_unavailable`, which pins that `MessageFormat` rejects `otlp_json` (not implemented). Routing/payload correctness (receiver-issue parity) additionally covers: a mixed-signal configuration exporting traces + metrics + logs on distinct topics in one run (`exports_all_three_signals_to_distinct_topics`); malformed/undecodable payloads per signal, which fail to encode, increment `messages{signal,failure}`, are not sent, and do not stall the loop (a following valid batch still delivers) -- `encoding_failure_increments_failure_metric_per_signal` (plus the existing `encoding_failure_is_permanently_nacked`); and transport-header propagation onto the produced Kafka record (`build_kafka_headers_propagates_transport_headers_under_policy`, including the format-header-collision skip, and `build_kafka_headers_writes_only_format_header_without_policy` for the default no-propagation path).)
- [x] **Acknowledgements.** `required_acks` = `None`/`One`/`All` mapped to `request.required.acks` `0`/`1`/`-1` (`config.rs:138-162`, applied at `config.rs:462`). (Done: the `None`->`0`/`One`->`1`/`All`->`-1` string mapping is unit-tested (`test_required_acks_as_kafka_value`) and asserted on the built client config (`first_class_fields_override_producer_config_passthrough`); end-to-end delivery is validated under the strongest and weakest settings by `exports_logs_with_acks_all_round_trips` (`acks=-1`) and `exports_logs_with_acks_none_round_trips` (fire-and-forget `acks=0`), each confirming the record round-trips and the export counter increments. `One` is the default exercised by every other round-trip.)
- [x] **Compression.** `gzip`/`snappy`/`lz4`/`zstd` mapped to `compression.type` (`config.rs:755-779`, applied at `config.rs:458`); `snappy`/`lz4` are noted as not end-to-end tested (`config.rs:753-754`). (Done: the codec-to-`compression.type` mapping is asserted on the built client config for all four codecs (`build_client_config_maps_each_compression_codec`), and each codec round-trips through the exporter (`exports_logs_gzip_round_trips`, `exports_logs_snappy_round_trips`, `exports_logs_lz4_round_trips`, `exports_logs_zstd_round_trips`: produce + consume the original decompressed payload + assert broker persistence), promoting `snappy`/`lz4` to round-trip-validated on the mock. NOTE: librdkafka/the consumer transparently decompresses and `ConsumedMessage` retains no codec field, so the on-wire codec is not observable on the mock; the tests validate acceptance + payload survival, not the wire codec.)
- [x] **TLS.** `ca_file` / `cert_file` / `key_file` / `key_password` / `insecure`, mTLS and server-only, mapped to `ssl.*` properties (`common/kafka/security.rs:119-136`; validation `common/kafka/mod.rs:166-198`). (Done at the config-mapping level: `apply_tls_config` writes `ssl.ca.location` / `ssl.certificate.location` / `ssl.key.location` / `ssl.key.password` / `enable.ssl.certificate.verification`, covered by the security.rs `apply_tls_config` tests (mTLS, CA-only, empty-block, `insecure`) and the mod.rs TLS validation matrix (`tls_validate_*`); the exporter cert-file validation is covered by `new_with_tls_config_fails_on_missing_certs` / `new_with_tls_insecure_fails_on_missing_certs`. A real TLS handshake cannot run on `MockCluster` (it performs no TLS termination -- see `common/kafka/test/README.md` Limitations), so a full handshake test is deferred to a real broker.)
- [x] **SASL.** `PLAIN`, `SCRAM-SHA-256`, `SCRAM-SHA-512`, and `AWS_MSK_IAM_OAUTHBEARER` (`common/kafka/auth.rs:14-32`); protocol selection in `resolve_security_protocol` (`common/kafka/security.rs:45-58`). (Done at the config-mapping level: `apply_sasl_config` sets `sasl.mechanism`/`sasl.username`/`sasl.password` (security.rs `sasl_config_sets_*` tests), `resolve_security_protocol` picks `SASL_SSL`/`SASL_PLAINTEXT`/`SSL`/`PLAINTEXT` across the TLS x auth matrix (security.rs `protocol_*` tests), and the auth.rs deserialization matrix pins each mechanism. A real SASL authentication handshake cannot run on `MockCluster` (README Limitations), so a full auth test is deferred to a real broker.)
- [x] **AWS MSK IAM.** OAUTHBEARER token generation and refresh via `generate_oauth_token` (spawns a std thread with a current-thread Tokio runtime to `block_on(generate_auth_token(region))`, `common/kafka/aws.rs:37-60`); `ENABLE_REFRESH_OAUTH_TOKEN = true` (`common/kafka/aws.rs:34`, `77`). Requires building with the `aws` feature (see section 8). (Done at the config-gating level, from section 8: the `aws`-gated `SaslMechanism::AwsMskIamOauthbearer` and `build_aws_msk_context` selection are covered feature-on (auth.rs/security.rs AWS tests) and feature-off (`deserialize_aws_msk_mechanism_rejected_without_aws_feature`). Live OAUTHBEARER token generation/refresh needs real AWS credentials and a real MSK broker, which `MockCluster` cannot exercise, so the token-refresh path is deferred to an integration environment with a recorded decision here.)

**Acceptance criteria:**

- [x] Broker-backed success and failure tests cover all supported encodings, the acks matrix, compression codecs, the TLS/SASL matrix, and AWS MSK IAM token refresh -- primarily via `MockCluster`, with a real broker where the mock cannot exercise the behavior. (Done: encodings (OTLP + OTAP for logs/traces/metrics, persistence, per-signal failure) and the acks and compression matrices are covered end-to-end on the mock broker (test names above); the TLS/SASL matrix and AWS MSK IAM token refresh are covered at the config-mapping/gating level, with real-broker handshake and token-refresh tests explicitly deferred because `MockCluster` performs no TLS/SASL handshake -- recorded in each item above and in `common/kafka/test/README.md` Limitations. Export outcomes are asserted via the migrated `exporter.kafka.exports.messages` counter partitioned by `signal` and `outcome`, read through the harness's `kafka_exports` measurement helper.)

## 7. Telemetry

Verify metric names, units and record counts, final shutdown snapshots, and safe structured events.

> Note: this section was written against the pre-migration flat metric schema. The exporter telemetry has since been migrated to enum attributes (PR #3638), so the items below are reconciled to the current two-set schema.

- [x] **Metric sets (post-migration).** The exporter now exposes two sets (`metrics.rs`): an operational `Counter<u64>` set `exporter.kafka` (`acks_received`, `nacks_received`, `topic_from_header`, `topic_from_static_config`, unit `{batch}`) and a measurement set `exporter.kafka.exports` whose single `messages` counter (unit `{message}`) is partitioned by the `SignalOutcomeAttributes` enum (`signal` in `logs`/`traces`/`metrics` x `outcome` in `success`/`failure`). This replaces the old flat `logs_exported` / `traces_failed` / ... counters. **There are still no gauges or up/down counters.** (Done: schema (both set names + the `{message}`/`{batch}` units) is asserted on the terminal snapshots by `terminal_snapshot_exposes_both_metric_sets_with_expected_units`; the operational counters are exercised end-to-end by `acks_received_counter_increments_on_downstream_ack`, `nacks_received_counter_increments_on_downstream_nack`, and `topic_source_counters_reflect_header_vs_static_routing`; the `messages{signal,outcome}` buckets by the section 6 encoding/failure tests and the delivery-semantics tests, read via the harness `kafka_exports` helper.)
- [x] **Unit vs. count semantics (recorded decision).** The migration relabeled the export counter to `messages` with unit `{message}` ("Number of exported messages"), but the exporter still increments it **once per pdata/batch** (`inc_exported` / `inc_failed` call `.inc()`), and `acks_received` / `nacks_received` are likewise per batch. Decision: the exporter counts **per batch/pdata**, matching the sibling nodes' outcome counters; the `{message}` unit is read as "a message produced to Kafka per exported batch" and is left as-is (no per-record counting). (Done: the per-batch semantics are pinned by `export_counts_one_per_batch_regardless_of_record_count` -- a single pdata carrying 25 log records increments the `messages{logs,success}` bucket by exactly 1 -- so a future change to per-record counting cannot regress this silently.)
- [x] **Final shutdown snapshot.** The terminal state carries `self.metrics.terminal_snapshots()` (`exporter.rs`, the `Shutdown` arm), covering both metric sets. (Done: `final_snapshot_reflects_all_activity_up_to_shutdown` drives N successful exports plus a downstream ack and asserts the terminal snapshot records `messages{logs,success} == N` and `acks_received == 1`, and a second exporter's snapshot records `messages{logs,failure} == 1` for a broker-rejected export, so the shutdown snapshot is confirmed to reflect all activity up to shutdown rather than a partial or reset view.)
- [x] **Safe structured events.** The node emits no structured events (README Events section); the `otel_*` / `effect_handler.info` log sites are enumerated in section 1 and all client-influenced values pass through `sanitize_for_log`. (Done: the sanitizer's exact escape/truncation behavior is pinned by the `sanitize_for_log_*` unit tests (section 1); the live nack-reason log path is exercised end-to-end by `nack_reason_with_control_characters_is_handled_safely`, which delivers a nack whose reason embeds control characters and an overlong tail and confirms the exporter still counts the nack and shuts down cleanly.)
- [ ] **Operational gauges gap.** No gauges exist for in-flight record count, producer queue depth, or end-to-end latency (`metrics.rs`). Propose adding the operationally important gauges or record a decision to defer.
**Acceptance criteria:**

- [x] Metric names, units, and record-vs-batch counting are reconciled and verified -- `metrics.rs`, exporter README Telemetry section. (Done: names/units verified by `terminal_snapshot_exposes_both_metric_sets_with_expected_units`; per-batch counting recorded and pinned by `export_counts_one_per_batch_regardless_of_record_count`.)
- [x] The final shutdown snapshot is verified and structured events/logs are confirmed safe -- exporter `Shutdown` arm, section 1 log sites. (Done: snapshot completeness by `final_snapshot_reflects_all_activity_up_to_shutdown`; log safety by the `sanitize_for_log_*` unit tests and `nack_reason_with_control_characters_is_handled_safely`.)
- [ ] Operational-gauge gaps are addressed or explicitly deferred with a recorded decision -- `metrics.rs`.

## 8. Configuration & packaging

Validate timeout limits, producer escape-hatch interactions, feature gates, and optional dependency isolation.

- [x] **Timeout limits.** `timeout_ms` is validated to reject `0` (which maps to librdkafka's infinite `message.timeout.ms`) and any value `> MAX_TIMEOUT_MS` (`config.rs:533-544`, `MAX_TIMEOUT_MS = 30_000` at `config.rs:722`); it maps to `message.timeout.ms` (`config.rs:454`). (Done: bounds covered by `test_try_from_zero_timeout_fails` (reject 0), `test_try_from_excessive_timeout_fails` (reject > 30s), `test_try_from_max_timeout_succeeds` (inclusive upper bound), and `test_try_from_default_timeout_succeeds` (serde default 5000 stays valid); the `message.timeout.ms` mapping itself is asserted on the built client config by the new `build_client_config_maps_timeout_to_message_timeout_ms`.)
- [x] **Producer escape-hatch interactions.** `producer_config` is applied first so built-in fields override conflicting keys (`build_client_config`, `config.rs:441-489`, order at `447-485`); overridden keys emit `otel_warn!("kafka.exporter.producer_config.overridden_key", ...)` (`exporter.rs:197-204`) using the managed-key list `MANAGED_PRODUCER_CONFIG_KEYS` (`config.rs:21-40`, checked via `overridden_producer_config_keys`, `config.rs:699-706`). `debug` and `log_level` are applied last to override the escape hatch (`config.rs:479-485`). Validate the precedence and the warning surface. (Done: the new `first_class_fields_override_producer_config_passthrough` asserts every managed first-class field (bootstrap.servers, client.id, message.timeout.ms, compression.type, request.required.acks, linger.ms, partitioner) wins over a conflicting passthrough in the built config and is reported for the warning; `unmanaged_producer_config_passes_through` confirms non-managed tuning knobs survive the escape hatch; `debug_field_overrides_producer_config_debug` and `log_level_first_class_is_applied` confirm the last-applied `debug`/`log_level` fields win. The warning surface (`overridden_producer_config_keys`) is covered by `overridden_keys_detects_conflicts` and `overridden_keys_empty_when_no_conflicts`; `allow.auto.create.topics` precedence + default-deny by `build_client_config_defaults_auto_create_to_false`, `build_client_config_allows_auto_create_when_opted_in`, `auto_create_key_is_managed_and_first_class_field_wins`. The `otel_warn!` emission itself is a global-telemetry side effect and is validated through its `overridden_producer_config_keys` input rather than by capturing the log line.)
- [x] **Feature gates & optional-dependency isolation.** `kafka-exporter` pulls `dep:rdkafka`, `dep:futures-channel`, `dep:futures-util`, `dep:xxhash-rust`, `dep:hex` (`Cargo.toml:81-87`); `aws` pulls `dep:aws-config`, `dep:aws-msk-iam-sasl-signer` (`Cargo.toml:88-91`). `aws` is **not** auto-enabled by `kafka-exporter`, so AWS MSK IAM requires building with both. Validate builds with `kafka-exporter` alone, `kafka-exporter + aws`, and neither, and that AWS-only types are correctly gated (e.g. `SaslMechanism::AwsMskIamOauthbearer`, `build_aws_msk_context` defense-in-depth at `common/kafka/security.rs:95-107`). (Done: builds verified for `kafka-exporter` alone, `kafka-exporter + aws`, and neither (`cargo check`/`cargo test` under each combination). AWS-only gating is exercised feature-on by `mechanism_as_rdkafka_str_aws`, `mechanism_is_username_password_aws`, `deserialize_mechanism_aws_msk`, `deserialize_aws_msk_backward_compatible`, the `resolve_security_protocol` AWS cases (`protocol_tls_and_auth`, `protocol_aws_msk_without_tls`), `sasl_config_sets_oauthbearer_for_aws_msk`, and the `build_aws_msk_context` matrix (`aws_msk_context_from_aws_auth`, `aws_msk_context_none_for_generic_sasl`, `aws_msk_context_none_for_plain_auth`, `aws_msk_context_none_when_no_auth`, `aws_msk_context_none_for_plain_with_aws_msk_block`); and feature-off by the new `deserialize_aws_msk_mechanism_rejected_without_aws_feature`, which asserts the MSK mechanism string is rejected (variant absent) without the `aws` feature.)

**Acceptance criteria:**

- [x] Timeout bounds, escape-hatch override precedence + warning, and validation rules are covered by tests -- `config.rs:441-544`, `exporter.rs:197-204`. (Done: timeout bounds + mapping -> `test_try_from_zero_timeout_fails`, `test_try_from_excessive_timeout_fails`, `test_try_from_max_timeout_succeeds`, `test_try_from_default_timeout_succeeds`, `build_client_config_maps_timeout_to_message_timeout_ms`; escape-hatch precedence + warning surface -> `first_class_fields_override_producer_config_passthrough`, `unmanaged_producer_config_passes_through`, `debug_field_overrides_producer_config_debug`, `log_level_first_class_is_applied`, `overridden_keys_detects_conflicts`, `overridden_keys_empty_when_no_conflicts`, and the three auto-create tests.)
- [x] Feature-gated builds (`kafka-exporter` alone, with `aws`, and disabled) compile and isolate optional dependencies correctly -- `Cargo.toml:81-91`. (Done: all three build combinations checked; AWS-only types gated feature-on/feature-off via the auth.rs and security.rs tests listed above, including the new `deserialize_aws_msk_mechanism_rejected_without_aws_feature`.)

## 9. Performance & scalability

Benchmark throughput, latency, polling overhead, and multi-core behavior with slow and unavailable brokers.

- [x] **Throughput / latency benchmarks** per encoding and per signal, at 1 and N partitions; the serial single-message send loop (`exporter.rs:524-530`) is the current ceiling and should be measured directly. (Done: `benches/exporters/kafka_exporter/throughput.rs` (Criterion, run against the in-process mock broker via the `kafka_bench`-gated `bench_support` facade; registered as the `kafka_exporter_throughput` `[[bench]]`) includes `bench_encoding_and_signal` -- OTLP and OTAP x logs/traces/metrics -- and `bench_max_in_flight_sweep` at 1 and 4 partitions. The `max_in_flight` sweep injects a small per-request broker round trip and times the send loop so the pipelining win is directly visible: for a 64-batch, single-partition run, `max_in_flight = 1` (serial) is ~280 ms, `= 5` is ~65 ms (about 4x), and `= 32` is ~150 us (the send loop never blocks). Absolute figures are mock-relative -- the custom producer polls delivery callbacks on a 1s interval, so a serial run's per-batch stall is dominated by that poll quantization rather than the injected latency -- but the relative scaling quantifies the section 4 concurrency direction. On a real broker the same mechanism overlaps genuine acknowledgement latency.)
- [x] **Polling overhead.** The custom producer runs a dedicated poll thread at a **1-second** interval as a workaround for high idle CPU in upstream rdkafka (`producer.rs:88-98`, rationale `README.md:542-544`); measure its idle and under-load overhead and confirm the interval is appropriate. (Done: `bench_poll_overhead` compares a saturated run against a single-batch run so the fixed per-run poll-thread cost is visible against the throughput profile.)
- [x] **Slow / unavailable brokers.** Measure behavior and pipeline-core impact when the broker is slow or unavailable (ties to section 4 backpressure and section 2 shutdown); confirm broker operations never stall the pipeline core beyond the configured deadline. (Done: `bench_unavailable_broker` drives the transient-nack path against an unroutable broker with a short `timeout_ms` and a short bounded shutdown, measuring the nack-path throughput; the deadline-boundedness itself is asserted by `shutdown_with_parked_pdata_and_stalled_broker_is_deadline_bounded` and the section 2 `shutdown_honors_deadline_when_broker_unavailable` tests.)
- [x] **Multi-core behavior.** Characterize scaling across cores/instances (the exporter node itself is single-task and `!Send`-bound to its thread). (Done/recorded decision: the exporter node is a single `!Send` task bound to its thread, so intra-node scaling is via delivery-future pipelining (`max_in_flight`, quantified by `bench_max_in_flight_sweep`) rather than multiple cores; horizontal scaling is via multiple node instances / more partitions. The benches characterize the single-instance ceiling and the pipelining gain; this is documented in the README "Backpressure and concurrency" section.)

**Acceptance criteria:**

- [x] Performance characteristics are documented via benchmarks (throughput, latency, poll overhead, multi-core), with slow/unavailable-broker behavior confirmed not to stall the pipeline core, and the concurrency direction from section 4 quantified -- `exporter.rs:524-530`, `producer.rs:88-98`. (Done: the four `benches/exporters/kafka_exporter/throughput.rs` groups cover throughput/latency per encoding+signal, the `max_in_flight` sweep at 1 and N partitions (quantifying the section 4 concurrency direction), poll overhead, and the unavailable-broker nack path; deadline-boundedness under a slow/unavailable broker is asserted by the section 2 and the `shutdown_with_parked_pdata_and_stalled_broker_is_deadline_bounded` tests; multi-core scaling is recorded as a single-task/`!Send` node scaled via `max_in_flight` and multiple instances. How to run: `cargo bench -p otap-df-contrib-nodes --features kafka_bench --bench kafka_exporter_throughput`.)

## Cross-cutting: integration test suite

Integration testing is done primarily against `rdkafka::mocking::MockCluster` (librdkafka's built-in mock), which runs **in-process** and in CI by default with no Docker dependency. The exporter drives a fully-wired node through the engine's `ExporterWrapper` (control channel, pdata channel, effect handler) and then consumes the produced records back from the mock broker to assert on topic, payload bytes, message-format header, and partition key. The current suite covers: per-signal OTLP happy path (traces/metrics/logs), OTAP encoding (`otap` format header + `BatchArrowRecords` decode), dynamic topic routing from a transport header, partition-key derivation from transport headers, graceful shutdown flushing buffered records, deadline-bounded shutdown against an unavailable broker, shutdown flushing under sustained traffic, live reconfiguration via `Config` build-and-swap, and delivery semantics (ack/nack propagation and classification, timeout-boundedness, key-to-partition stability, null-key round-robin, per-partition ordering, and recovery across a broker restart / leader reassignment) (`exports_logs_otlp_to_mock_broker`, `exports_traces_otlp_to_mock_broker`, `exports_metrics_otlp_to_mock_broker`, `exports_logs_otap_sets_otap_format_header`, `routes_to_topic_from_transport_header`, `sets_partition_key_from_transport_headers`, `shutdown_flushes_buffered_records`, `shutdown_honors_deadline_when_broker_unavailable`, `shutdown_flushes_under_sustained_traffic`, `reconfigure_switches_topic`, `reconfigure_flushes_inflight_before_swap`, `reconfigure_with_invalid_config_keeps_running`, `send_success_reports_ack`, `send_failure_reports_transient_nack`, `delivery_success_increments_exported`, `produce_failure_increments_failed`, `send_times_out_within_bound_when_broker_unavailable`, `same_partition_key_maps_to_stable_partition`, `null_key_distributes_evenly_across_partitions`, `preserves_per_partition_order`, `recovers_across_broker_restart_and_leader_reassignment`, `produce_failure_is_bounded_and_not_persisted_on_mock`, `disallowed_dynamic_topic_is_permanently_nacked`, `allowed_dynamic_topic_is_delivered`), and retry correctness (error classification, retry exhaustion / drop-at-source, and bounded duplicate/ordering, validated end-to-end via the harness Ack/Nack unwind channel with a stand-in retry processor) (`encoding_failure_is_permanently_nacked`, `transient_nack_reaches_retry_on_send_failure`, `permanent_nack_reaches_retry_on_disallowed_topic`, `transient_nack_retried_until_success_then_acked`, `permanent_nack_is_not_retried`, `retried_transient_send_duplicates_bounded`). Configuration and packaging (section 8) are covered by unit tests in `config.rs`, `common/kafka/auth.rs`, and `common/kafka/security.rs` rather than the mock broker -- timeout bounds and `message.timeout.ms` mapping, producer escape-hatch precedence (managed first-class fields win; unmanaged keys pass through; `debug`/`log_level` applied last), the overridden-key warning surface, and feature-gated AWS MSK IAM isolation (feature-on gating plus feature-off rejection) -- enumerated in the section 8 notes above. Kafka-integration coverage (section 6) adds the OTAP encoding path for traces and metrics (`exports_traces_otap_sets_otap_format_header`, `exports_metrics_otap_sets_otap_format_header`), broker-side persistence for both encodings (`otlp_and_otap_payloads_persist_on_broker`), per-signal produce-failure classification (`logs_send_failure_increments_failed`, `traces_send_failure_increments_failed`, `metrics_send_failure_increments_failed`), the acks matrix end-to-end (`exports_logs_with_acks_all_round_trips`, `exports_logs_with_acks_none_round_trips`), a per-codec compression round-trip (`exports_logs_gzip_round_trips`, `exports_logs_snappy_round_trips`, `exports_logs_lz4_round_trips`, `exports_logs_zstd_round_trips`), and the not-implemented-format guard (`otlp_json_message_format_is_unavailable`); export outcomes are read from the migrated `exporter.kafka.exports.messages` counter (partitioned by `signal`/`outcome`) via the harness `kafka_exports` helper. TLS/SASL/AWS-MSK remain config-mapping/gating-level because `MockCluster` performs no TLS/SASL handshake. Telemetry coverage (section 7) adds terminal-snapshot schema/unit verification (`terminal_snapshot_exposes_both_metric_sets_with_expected_units`), per-batch counting semantics (`export_counts_one_per_batch_regardless_of_record_count`), the operational ack/nack counters (`acks_received_counter_increments_on_downstream_ack`, `nacks_received_counter_increments_on_downstream_nack`), the topic-source counters (`topic_source_counters_reflect_header_vs_static_routing`), adversarial nack-reason log safety (`nack_reason_with_control_characters_is_handled_safely`), and shutdown-snapshot completeness (`final_snapshot_reflects_all_activity_up_to_shutdown`); these use the new `KafkaExporterHarness::send_ack` / `send_nack` helpers to drive the downstream Ack/Nack accounting path. Routing/payload and failure-recovery parity with the receiver issue adds a mixed-signal export (`exports_all_three_signals_to_distinct_topics`), per-signal malformed-payload handling with the failure metric and loop survival (`encoding_failure_increments_failure_metric_per_signal`), transport-header propagation onto the record (`build_kafka_headers_propagates_transport_headers_under_policy`, `build_kafka_headers_writes_only_format_header_without_policy`), and prolonged-outage recovery (`recovers_after_prolonged_produce_outage`). The exporter is a sink, so the receiver's offset-guarantee and consumer-group-rebalancing areas have no analog here.

- [x] Extend coverage on top of the mock broker for the security-constraint, shutdown/reconfigure, retry-processor, backpressure, delivery-semantics, auth/TLS, and telemetry scenarios called out above. (Done for backpressure/pipelining: a new mock-broker suite covers bounded delivery-future pipelining -- `default_max_in_flight_preserves_serial_ordering`, `pipelined_sends_all_deliver_and_ack`, `pipelined_preserves_per_partition_order_with_keys`, `bounded_concurrency_caps_in_flight_and_delivers`, `parked_pdata_is_enqueued_before_shutdown`, `shutdown_with_parked_pdata_and_stalled_broker_is_deadline_bounded`, `shutdown_drains_pipelined_in_flight_sends`, `reconfigure_drains_pipelined_in_flight_before_swap`, `prolonged_outage_keeps_pipelined_sends_bounded`, `purged_pipelined_send_is_transiently_nacked_with_pdata`, `enqueue_failure_reports_nack_without_tracking`, `pipelined_export_counts_one_per_batch` -- plus config-validation tests `max_in_flight_zero_is_rejected`, `positive_max_in_flight_is_accepted`, `default_max_in_flight_is_one`. Documented mock limitations: deterministic `QueueFull` and a deterministic per-send outage/recovery split cannot be forced on the in-process mock and require a real broker.)

**Acceptance criteria:**

- [x] The mock broker suite stays green in CI and is extended to cover the security, shutdown/reconfiguration, retry, backpressure, delivery-semantics, auth/TLS, and telemetry scenarios above, with a real broker used only where the mock cannot exercise the behavior. (Done for the backpressure/pipelining scenarios: the 15 tests listed above are green on the mock broker; where the mock cannot exercise a behavior deterministically (queue-full rejection, per-send outage/recovery mapping) the limitation is documented in the test and deferred to a real broker.)

