# Dependencies for dashboard.py itself (validate, build, serve, run).
#
# The `run` verb additionally spawns the orchestrator as a subprocess; that
# requires the orchestrator's own deps installed in the same Python env --
# see tools/pipeline_perf_test/orchestrator/requirements.txt.

pyyaml==6.0.3
jinja2==3.1.6
