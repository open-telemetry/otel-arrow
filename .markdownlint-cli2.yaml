# Rule configuration
config:
  # MD013/line-length
  MD013:
    code_blocks: false
    tables: false
    headings: false

# CHANGELOG files are auto-generated by chloggen and may contain long
# issue-link lines and multiple blank lines that cannot be controlled.
ignores:
  - "**/CHANGELOG.md"
