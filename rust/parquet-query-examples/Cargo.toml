[package]
name = "parquet-query-examples"
version = "0.1.0"
edition = "2024"

[dependencies]
arrow = { version = "58.0.0", features = ["prettyprint", "chrono-tz"] }
datafusion = "53.0.0"

tokio = {version = "1", features = ["full"] }


