[package]
name = "data_engine_columnar"
version.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
publish.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
arrow = { workspace = true }
thiserror = { workspace = true }

# TODO consider moving all this stuff back to workspace
async-trait = "0.1.88"
env_logger = "0.11.8"
log = "0.4"
prost = "0.14"
datafusion = { version = "50.2", default-features = false}
data_engine_expressions = { path = "../expressions" }
data_engine_kql_parser = { path = "../kql-parser" }
otap-df-otap = { path = "../../../otap-dataflow/crates/otap" }
otap-df-pdata = { path = "../../../otap-dataflow/crates/pdata"}
otel-arrow-rust = { path = "../../../otel-arrow-rust" }

[dev-dependencies]
# TODO consider moving this to the workspace
tokio = { version="1", features=["full"] }