version: otel_dataflow/v1
engine: { }
groups:
  default:
    pipelines:
      main:
        # Shows the batch processor.
        policies:
          channel_capacity:
            control:
              node: 100
              pipeline: 100
            pdata: 128

        nodes:
          receiver:
            type: receiver:traffic_generator
            config:
              traffic_config:
                max_batch_size: 10
                signals_per_second: 1000
                log_weight: 100
              registry_path: https://github.com/open-telemetry/semantic-conventions.git[model]
          batch:
            type: processor:batch
            config:
              otap:
                # At the rate set above (100/s), min_size=1000 would emit a batch
                # every 10 seconds.
                min_size: 1000
                sizer: items
              # The duration causes pending data to flush after 3s.
              max_batch_duration: 3s
          debug:
            type: processor:debug
            config:
              verbosity: basic
              mode: signal
          noop:
            type: exporter:noop
            config:

        connections:
          - from: receiver
            to: batch
          - from: batch
            to: debug
          - from: debug
            to: noop
