change_type: enhancement
component: otap
note: Re-export the `arrow` crate from pdata as `otel_arrow_dfe_pdata::arrow` so downstream code can build against the exact Arrow version pdata uses
issues: [4078]
subtext:
