change_type: bug_fix
component: otap
note: Read the metric id column optionally so an OTAP metrics batch that omits the id column ingests instead of failing the whole view build
issues: [4116]
subtext:
