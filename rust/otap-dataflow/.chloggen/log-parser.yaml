# Use this changelog template to create an entry for release notes.

# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: new_component

# Must be one of the components listed in config.yaml.
component: pipeline

# A brief description of the change.
note: Add a dedicated log parser processor for configurable regex, JSON and headerless CSV parsing of framed log bodies.

# Tracking issue or PR number is assigned when this change is submitted.
issues: []

subtext: Map body, RFC 3339 event time and severity atomically while preserving malformed records, provenance and downstream delivery completion. Configure explicit input and scratch limits.
