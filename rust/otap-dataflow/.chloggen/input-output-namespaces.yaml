change_type: breaking
component: engine
note: "Rename node and flow metric scopes to input/output, add flow messages and size, and report durations as seconds histograms."
issues: [2884]
subtext: |
  Migration: update node and flow scope selectors and flow metric configuration values from consumed/produced to input/output.
