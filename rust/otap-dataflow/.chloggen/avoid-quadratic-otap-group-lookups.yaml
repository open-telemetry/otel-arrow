change_type: enhancement
component: otap
note: Avoid quadratic resource and scope scans in the OTAP trace view by resolving each resource's and scope's representative row from the row group already computed during grouping, instead of rescanning the groups on every accessor call.
issues: [3936]
subtext:
