change_type: breaking
component: pipeline
note: >-
  Migrated parquet exporter telemetry from flat counters to dimensioned metric populations with enum attributes.
issues: [3530]
subtext: |
  Migration: Update queries: flat file metrics -> files.count{operation}, rows_written -> rows.written.
