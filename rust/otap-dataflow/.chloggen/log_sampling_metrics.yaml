change_type: breaking
component: pipeline
note: Refactored log sampling metrics to use standard `processor.log_sampling.dropped_items` metric instead of separate consumed/dropped counters.
issues: [3530]
subtext: |
  Migration: Update dashboards, alerts, and queries to use the new `processor.log_sampling.dropped_items` metric (with `signal="logs"`) instead of `log_signals_consumed` and `log_signals_dropped`.
