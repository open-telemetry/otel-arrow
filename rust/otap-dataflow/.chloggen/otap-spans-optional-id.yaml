change_type: bug_fix
component: otap
note: Read the span id column optionally so a spans-only OTAP batch that omits the id column ingests instead of failing the whole view build
issues: [4115]
subtext:
