change_type: breaking

component: pipeline

note: "Modernize Geneva exporter metrics with shared attempt telemetry and bounded signal, outcome, error, and skip attributes."

issues: [3530, 3822]

subtext: |
  Migration: Replace `otap.exporter.geneva` queries with `exporter.attempted` for batch uploads and `exporter.geneva.{encoding,failures,skipped}` for diagnostics. Durations now use seconds.
