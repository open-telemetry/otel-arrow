change_type: enhancement

component: engine

note: Add the `VendorBundle` capability so an agent-fed extension can hand an opaque, vendor-defined attribute bag (a JSON object) to data-path nodes.

issues: [3477]

subtext: |
  The capability carries host-resolved, vendor-defined attributes through
  data-path nodes, allowing consumers to skip a config-service handshake.
