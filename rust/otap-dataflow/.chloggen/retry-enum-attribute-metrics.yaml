# Use this changelog template to create an entry for release notes.

change_type: breaking
component: pipeline
note: Replace retry processor metric-name dimensions with bounded enum attributes, add retry recovery and termination metrics, and use engine-owned item outcome metrics.
issues: [3300]
subtext: Local retry scheduling failures are now forwarded upstream instead of being routed back to the retry processor.
