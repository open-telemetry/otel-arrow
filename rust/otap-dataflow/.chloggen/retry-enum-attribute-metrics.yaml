# Use this changelog template to create an entry for release notes.

change_type: breaking
component: pipeline
note: Replace retry processor metric-name dimensions with a bounded signal attribute and use engine-owned item outcome metrics.
issues: [3300]
subtext:
