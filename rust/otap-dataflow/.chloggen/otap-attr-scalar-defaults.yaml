change_type: bug_fix
component: otap
note: Decode omitted scalar attribute columns as their type default, so int, double, bool, string and bytes read back as 0, 0.0, false, empty string and empty bytes instead of Empty
issues: [4111]
subtext:
