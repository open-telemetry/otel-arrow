change_type: breaking
component: engine
note: "Rename node metrics to node.consumed.* and node.produced.* and report duration histograms in seconds."
issues: [2884]
subtext: |
  Migration: Replace `node.consumer.consumed.*` with `node.consumed.*` and `node.producer.produced.*` with `node.produced.*`. Update scope selectors and treat duration values as seconds.
