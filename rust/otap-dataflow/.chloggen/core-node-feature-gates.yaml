change_type: breaking
component: pipeline
note: Add per-node Cargo features for core receivers, processors, and exporters while preserving the complete default build.
issues: [4085]
subtext: "Migration: Consumers using `otel-arrow-dfe-core-nodes` with `default-features = false` must enable `core-nodes` for the previous complete set or select the required node features."
