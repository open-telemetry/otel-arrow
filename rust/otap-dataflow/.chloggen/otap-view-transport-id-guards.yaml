change_type: bug_fix
component: pdata
note: "Prevent native OTAP views from silently indexing transport-optimized IDs"
issues: [2824]
subtext: "Wire-facing consumers decode OTAP IDs only when needed, including empty child batches. Resource-only consumers avoid decoding unrelated payloads, and direct view construction returns a typed error for encoded IDs."
