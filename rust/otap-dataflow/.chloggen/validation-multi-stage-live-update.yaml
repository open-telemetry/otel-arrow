# Use this changelog template to create an entry for release notes.

# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The name of the component, or a single word describing the area of concern, (e.g. engine, otap).
# Must be one of the components listed in rust/otap-dataflow/.chloggen/config.yaml.
component: engine

# A brief description of the change.  Surround your text with quotes ("") if it needs to start with a backtick (`).
note: Add multi-stage validation scenarios that transition between stages using the live-update (reconfigure) API instead of restarting the engine.

# Mandatory: One or more tracking issues related to the change. You can use the PR number here if no issue exists.
issues: [3251]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the document.
# Use pipe (|) for multiline entries.
subtext: |
  The validation framework adds `Scenario::add_stage` with a `Stage` builder.
  Stages are transitioned in place by reconfiguring the pipelines via the admin
  reconfigure API, so one engine process is reused across stages.
