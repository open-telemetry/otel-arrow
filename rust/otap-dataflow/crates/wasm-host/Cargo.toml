[package]
name = "otel-arrow-dfe-wasm-host"
description = "WASM host-kernel runtime for OTAP dataflow processor plugins (experimental)"
version.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
publish.workspace = true
edition.workspace = true
rust-version.workspace = true

[features]
# The WASM host runtime is experimental and OFF by default. Enabling `wasm`
# pulls in wasmtime and registers the `wasm_processor` factory. The WIT
# contract and host surface are unstable and subject to breaking changes while
# the binary plugin system is in development.
default = []
wasm = ["dep:wasmtime"]

[dependencies]
otel-arrow-dfe-config.workspace = true
otel-arrow-dfe-engine.workspace = true
otel-arrow-dfe-otap.workspace = true
otel-arrow-dfe-pdata.workspace = true
otel-arrow-dfe-telemetry.workspace = true
otel-arrow-dfe-telemetry-macros.workspace = true
arrow.workspace = true
arrow-cast.workspace = true
arrow-select.workspace = true
wasmtime = { workspace = true, optional = true }
async-trait.workspace = true
linkme.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
otel-arrow-dfe-engine = { workspace = true, features = ["test-utils"] }
otel-arrow-dfe-pdata = { workspace = true, features = ["testing"] }
tokio.workspace = true

# The end-to-end integration test drives the wasmtime runtime, so it only
# builds when the experimental `wasm` feature is enabled.
[[test]]
name = "integration"
path = "tests/integration.rs"
required-features = ["wasm"]

[lints]
workspace = true
