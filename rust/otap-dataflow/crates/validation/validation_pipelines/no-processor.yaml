nodes:
  receiver:
    kind: receiver
    plugin_urn: "otlp:receiver"
    out_ports:
      out_port:
        destinations:
          - exporter
        dispatch_strategy: round_robin
    config:
      protocols:
        grpc:
          listening_addr: "127.0.0.1:4317"

  exporter:
    kind: exporter
    plugin_urn: "otlp:exporter"
    config:
      grpc_endpoint: "http://127.0.0.1:4318"
connections:
  - from: receiver
    to: exporter
