# Use this changelog template to create an entry for release notes.

# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: bug_fix

# The name of the component, or a single word describing the area of concern, (e.g. pkg/otel/arrow).
# Must be one of the components listed in go/.chloggen/config.yaml.
component: pkg/otel/arrow

# A brief description of the change.  Surround your text with quotes ("") if it needs to start with a backtick (`).
note: Return an error instead of panicking when a batch contains more than 65535 records.

# Mandatory: One or more tracking issues related to the change. You can use the PR number here if no issue exists.
issues: [1883]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the document.
# Use pipe (|) for multiline entries.
subtext: |
  The uint16 record-ID counters used by the metrics, logs, and traces producers
  previously wrapped past their maximum value, causing the delta ID builder to
  panic. These builders now return ErrTooManyRecords ("OTel Arrow components do 
  not currently support batch sizes larger than 65535").
